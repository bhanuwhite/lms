'use strict';

/**
 * content-library controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::content-library.content-library');
